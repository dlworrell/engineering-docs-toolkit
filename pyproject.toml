[project]
name = "engineering-docs-toolkit"
version = "0.1.0"
description = "Reusable build tooling for engineering documentation and translated technical books"
requires-python = ">=3.11"

[project.scripts]
edt = "edt.cli:main"

[tool.setuptools]
packages = ["edt"]
